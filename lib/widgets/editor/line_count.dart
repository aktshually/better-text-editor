import 'package:flutter/material.dart';

class LineCount extends StatelessWidget {
  const LineCount({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [Text("1")]);
  }
}
